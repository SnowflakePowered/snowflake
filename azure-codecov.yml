steps:
  - script: |
      bash <(curl -s https://codecov.io/bash) -v -s "$(System.DefaultWorkingDirectory)/src/Snowflake.Framework.Tests/TestResults"
    displayName: Publishing test coverage to Codecov 
    condition: not(variables['build.skiptest'])
