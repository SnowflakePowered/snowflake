class Snowflake(snowflakeUrl); {



    var snowflakeUrl = "http://localhost:30001";

    var loadGames = function () { };
    var loadPlatforms = function () { };

    var getGames = function () {
        return games;
    }
    var getPlatforms = function () {
            return platforms;
        }

    var platforms = {};
   var games = {};
}