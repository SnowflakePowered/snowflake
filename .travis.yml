language: csharp
sudo: false
solution: Snowflake.sln
mono: none
dotnet: 1.0.0-preview2-003121

install:
  - nuget restore ./Snowflake.sln
  
script:
  -  dotnet build ./Snowflake.sln
  
notifications:
  irc:
    channels:
      - "irc.stormbit.net#snowflake"
    on_success: always
    on_failure: always
    use_notice: false
    skip_join: true
  webhooks: http://fure-kun.herokuapp.com/hubot/travis?room=%23snowflake
